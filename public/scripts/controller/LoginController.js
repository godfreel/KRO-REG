define(['appModule'], function(KRO_REG)
{
    KRO_REG.lazy.controller('LoginController',
    [
        '$scope',
        'l10n', 
        '$location',
        'AuthService',
        '$rootScope',

        function($scope, l10n, $location, AuthService, $rootScope) {
	
			
		}
    ]);
});